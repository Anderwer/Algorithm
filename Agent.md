# Agent.md

## 目标

本文件用于约束后续在本仓库中的代码生成、改题、补模板、写题解式注释等行为。  
当 Agent 在 `Algorithm` 仓库内工作时，应优先模仿作者在 `Codeforces` 与 `algorithm` 目录中的**稳定代码习惯**，而不是使用通用工程风格。

---

## 一、总原则

1. **以竞赛风格优先**
   - 代码首先服务于算法竞赛提交，而不是工程化生产环境。
   - 优先考虑：
     - 书写速度
     - 思路清晰
     - 常数合理
     - 结构统一
     - 易于现场改动
   - 不要为了“现代 C++ 工程美观”强行引入复杂封装。

2. **尽量贴近作者已有写法**
   - 如果仓库中已有同类模板或同类题型写法，优先沿用。
   - 新代码应看起来像是和原仓库同一个人写的。

3. **默认使用 C++ 竞赛模板风格**
   - 默认头文件：
     `#include <bits/stdc++.h>`
   - 默认命名空间：
     `using namespace std;`
   - 默认类型别名：
     `using i64 = long long;`

4. **先正确，再简洁**
   - 在题解代码中，优先保证思路正确和实现稳健。
   - 若实现稍长但更符合作者一贯风格，则不要为了“短”而改成不熟悉的写法。

---

## 二、作者稳定代码风格总结

### 1. 文件整体结构

作者最常见的题目代码结构如下：

1. `#include <bits/stdc++.h>`
2. `using i64 = long long;`
3. `using namespace std;`
4. 常量 / 全局数组 / 全局图结构
5. 辅助函数
6. `solve()`
7. `main()`
8. 末尾大块注释，解释核心思路

Agent 后续写题时，默认遵循这个结构。

---

### 2. `solve()` / `main()` 习惯

#### 标准形式

- 绝大多数题使用 `void solve()`
- `main()` 中固定写法：

```cpp
int main()
{
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
    int t = 1;
    cin >> t;
    while(t--){
        solve();
    }
    return 0;
}
```

#### 例外

- 单组数据时，也常保留：
  - `int t = 1;`
  - 注释掉 `cin >> t;`
- 若全局使用了 `#define int long long`，则可能写 `signed main()`

#### 约束

- 没有明确理由时，优先保留 `solve()` + `main()` 双层结构
- 不要把所有逻辑直接塞进 `main()`
- 不要写成工程风格的类封装入口

---

### 3. 类型与整数习惯

1. 默认保留：
   - `using i64 = long long;`

2. 作者有两种风格，均真实存在：
   - 风格 A：普通 `int` + 局部 `i64`
   - 风格 B：`#define int long long`

3. 选择规则：
   - 若题目数值范围明确需要全局 64 位，并且代码逻辑适合统一处理，可使用：
     `#define int long long`
   - 否则优先使用：
     - 下标、点编号、次数等用 `int`
     - 答案、乘法、中间量用 `i64`

4. 模板中常见 `int / i64` 混用是作者习惯的一部分，不必过度“洁癖式统一”。

---

### 4. 命名习惯

作者命名偏竞赛化、短而稳定：

#### 常见变量名
- `n, m, q, t`
- `u, v, x, y, l, r`
- `a, b, c`
- `ans, res, sum, cnt`
- `fa, dep, siz, son, top`
- `g, tr, f, p, dis, vis`
- `mx, mn, now`
- `mod, p, N, M`

#### 习惯特征
1. 短变量名很多，但通常**语义仍可辨认**
2. 图常命名为：
   - `g`
   - `tr`
   - `G`
3. DP 常命名为：
   - `f`
   - `g`
4. 辅助数组常命名为：
   - `cnt`
   - `mx`
   - `mn`
   - `inv`
   - `nxt`

#### 要求
- 不要强行改成冗长英文名，如 `numberOfVertices`, `adjacencyListOfTree`
- 保持竞赛命名风格
- 但也不要滥用完全不可读的单字母变量覆盖所有对象

---

### 5. 缩进、空格、花括号风格

#### 缩进
- 使用 4 空格缩进

#### 花括号
- 函数与控制语句的大括号**另起一行**
- 常见写法：

```cpp
void solve()
{
    ...
}
```

```cpp
if(x)
{
    ...
}
```

#### 空格习惯
- `for(int i = 1; i <= n; i++)`
- `if(x == y) return;`
- `while(t--){ ... }` 这种写法也经常出现
- 运算符两侧通常保留空格
- 模板参数写作：
  `vector<int> a(n + 1);`

#### 单行语句
- 作者会适度保留单行写法，例如：
  - `if(v == fa[u]) continue;`
  - `for(int i = 1; i <= n; i++) cin >> a[i];`
- Agent 可以模仿，但不要把整段复杂逻辑都压成单行

---

### 6. 注释风格

这是作者代码风格中非常重要的一部分。

#### 题目代码中的注释特点

1. **核心解释常写在文件末尾的大块注释里**
2. 注释偏“题解式”，强调：
   - 观察
   - 转化
   - 状态定义
   - 转移来源
   - 为什么正确
3. 注释语言通常为中文
4. 注释语气常见：
   - `注意到`
   - `观察发现`
   - `分析得`
   - `设 f[i] ...`
   - `因此`
   - `综上`
   - `最后答案即为`
5. 注释不追求简短，而追求把关键思路讲顺

#### 模板中的注释特点

1. 模板名会直接写在文件顶部，如：
   - `// 普通字典树`
   - `//重链剖分`
2. 会说明函数用途、参数意义
3. 有时有“完整模板代码”或“可持久化版本”等补充说明

#### Agent 的要求

- 对于竞赛题代码：
  - 如果题有一定思维量，尽量补一段文件末尾的大注释
  - 注释重点解释“为什么这样做”
- 对于模板：
  - 注释应说明模板用途、复杂度、关键字段含义
- 不要写成英文论文风
- 不要写成过度工程化的 API 文档风
- 不要删除作者风格中的中文思路注释

---

## 三、常见实现偏好

### 1. 容器偏好

作者常用：

- `vector`
- `array`
- `set`
- `map`
- `queue`
- `priority_queue`

图结构常写：

- `vector<vector<int>> g(n + 1);`
- 或全局：
  `vector<int> g[N];`

树 / 图题中两种都常见：
- 局部动态容器
- 全局静态数组 + 邻接表

选择规则：
- 多测且总规模大、初始化要求高：可用全局数组
- 普通单题实现：`vector` 很常见

---

### 2. 全局数组偏好

作者非常常用全局数组，例如：

- `const int N = 2e5 + 10;`
- `int a[N], b[N], cnt[N];`
- `vector<int> g[N];`

要求：
1. 若题型很竞赛标准，优先允许全局数组写法
2. `N` 常取：
   - `2e5 + 10`
   - `1e6 + 10`
   - 根据题意略放大
3. 不要过度执着于局部封装一切

---

### 3. DFS / 树递归偏好

作者在树题中非常偏好直接 DFS：

- `dfs(u)`
- `dfs(u, fa)`
- `dfs1 / dfs2 / dfs3`

常见命名：
- `dfs`
- `dfs1`
- `dfs2`
- `dfs3`

要求：
- 树形 DP、重链剖分、LCA 等题优先沿用这种命名
- 若确实有多个阶段，使用 `dfs1/dfs2/dfs3` 很符合作者习惯
- 不必硬改成 `computeSubtree`, `rerootDP` 这类工程名

---

### 4. DP 风格

作者习惯先在注释中明确状态定义，再实现。

常见写法：
- `vector<int> f(n + 1, 0);`
- `vector f(1 << n, vector<int>(n, 1E9 + 10));`
- 全局 `f[N]`

要求：
1. 若是 DP 题，优先在注释中写出：
   - `设 f[i] ...`
   - `设 f[i][j] ...`
2. 转移应尽量直接
3. 若有滚动、优化，也应保留作者可读性优先的风格

---

### 5. 数据结构模板风格

作者模板的显著特点：

1. **偏竞赛模板化，而不是工业封装**
2. 常写成：
   - `template<typename T> struct Fenwick`
   - `template<class Node> struct SegmentTree`
   - `struct DSU`
   - `struct Hash`
3. 字段名简洁直接
4. 初始化常用：
   - `init(n)`
   - 构造函数中调用 `init`

要求：
- 写新模板时优先沿用这一风格
- 模板内部字段可以直接暴露，不必刻意私有化
- 函数名优先短小：
  - `init`
  - `add`
  - `merge`
  - `query`
  - `modify`
  - `range_query`
  - `range_modify`
  - `find`
  - `same`

---

### 6. 线段树与树状数组风格

#### Fenwick
作者喜欢的接口风格：
- `init`
- `add`
- `internalQuery`
- `get`
- `select`

#### SegmentTree
作者喜欢：
- `build`
- `pushup`
- `pushdown`
- `modify`
- `query`
- `range_modify`
- `range_query`
- `init_lazy`
- `lazy_union`
- `cal_lazy`

要求：
- 若写线段树模板，尽量贴近这些命名
- 若题目需要特殊 `Node` 信息，优先在 `Node` 中显式维护字段
- 不要引入过于抽象的泛型元编程风格

---

### 7. 并查集风格

作者的 DSU 模板特征：

- `vector<int> siz;`
- `vector<int> f;`
- `find`
- `same`
- `merge`
- `size`

并且保留了**可回退并查集**写法：
- `his`
- `time`
- `revert`

要求：
- 写并查集相关代码时优先沿用这套接口风格
- 若需要可回退，直接保留 `his + revert` 结构

---

### 8. 哈希、Trie、LCA 等模板偏好

#### Hash
- 使用结构体封装
- 维护正反哈希
- 双模数
- `init` + `getHash` + `getRevHash`

#### Trie
- 普通 Trie 常用全局二维数组
- 01 Trie 常用结构体 + `vector<array<int, 2>>`

#### LCA / 树剖
- 接受较传统模板风格
- `fa, dep, top, sz, son`
- `dfs1`, `dfs2`, `LCA`

要求：
- 这些领域若新增模板，优先继承作者原有接口与字段命名

---

## 四、题解代码生成规则

### 1. 默认代码模板

若无特别要求，生成题解代码时默认使用：

- `#include <bits/stdc++.h>`
- `using i64 = long long;`
- `using namespace std;`
- `solve()`
- `main()`
- 末尾中文大注释

---

### 2. 输入输出风格

默认：

```cpp
std::ios::sync_with_stdio(false);
std::cin.tie(nullptr);
```

输出上：
- 常用 `"\n"` 而不是 `endl`
- 批量输出时常直接循环 `cout << ...`

---

### 3. 多测处理

如果题目是多组数据：
- 使用 `int t = 1; cin >> t; while(t--){ solve(); }`

如果题目是单组数据：
- 也可以保留 `int t = 1;`
- 然后注释掉 `cin >> t;`

---

### 4. 思路注释要求

当题目不是纯模板题、不是纯签到题时，尽量在代码末尾添加一段中文注释，模板如下：

- 先写关键观察
- 再写状态定义 / 贪心依据 / 转化依据
- 再写转移或统计方法
- 最后写答案组成

注释风格应接近作者现有习惯，例如：

- `观察发现`
- `注意到`
- `设 f[i] ...`
- `因此我们可以`
- `综上`
- `最后答案即为`

---

### 5. 简单题处理

如果是非常简单的 A/B 题：
- 可以不强行写大段注释
- 但整体结构仍尽量保持统一

---

## 五、模板文件编写规则

### 1. 模板优先“可直接抄用”

模板的目标是：
- 下次做题时能快速复制
- 结构稳定
- 容易局部改动

因此：
- 不追求过度抽象
- 不追求隐藏实现细节
- 优先保留竞赛常见字段与函数名

---

### 2. 模板文件应包含什么

视情况包含：

1. 模板名 / 用途说明
2. 核心代码
3. 关键字段解释
4. 若有多版本，可并列给出
5. 若值得补充，可加入复杂度或适用场景

---

### 3. 模板语言风格

- 中文说明优先
- 代码内注释简洁直接
- 不要写成教科书式冗长废话
- 也不要完全无注释，导致下次难以回忆

---

## 六、与作者风格不符的做法

后续写代码时，应尽量避免以下行为：

1. **过度工程化**
   - 不要强行拆分多个类与命名空间
   - 不要引入复杂设计模式

2. **过度现代 C++ 炫技**
   - 不要无必要地使用复杂模板元编程
   - 不要为了“优雅”牺牲现场可改性

3. **命名过长**
   - 避免：
     - `calculateMaximumReachableSubtreeContribution`
     - `adjacencyRepresentation`
   - 应更接近：
     - `dfs`
     - `cnt`
     - `ans`
     - `mx`

4. **没有中文思路注释**
   - 对中等及以上思维量题，不要只给裸代码

5. **完全背离作者原模板接口**
   - 例如已有 `Fenwick::add/get`，就不要改成另一套陌生 API

6. **为了“规范”而消灭作者个人习惯**
   - 包括：
     - `dfs1/dfs2`
     - `f/g`
     - 全局数组
     - 文件末尾题解注释

---

## 七、当 Agent 需要在本仓库写新代码时的执行顺序

1. 先判断题型：
   - 图论
   - 树
   - DP
   - 数据结构
   - 数论
   - 字符串

2. 先回忆仓库已有模板与常见写法
3. 优先使用作者已有字段名与模板接口
4. 组织成：
   - 全局/辅助函数
   - `solve()`
   - `main()`
5. 若题目有一定思维量，补文件末尾中文注释
6. 检查代码整体观感是否“像作者自己写的”

---

## 八、可直接遵守的代码风格清单

### 必须优先遵守
- 使用 `#include <bits/stdc++.h>`
- 使用 `using i64 = long long;`
- 使用 `using namespace std;`
- 使用 `solve() + main()` 结构
- 使用竞赛风格短变量名
- 使用中文思路注释
- 贴近仓库既有模板接口

### 一般建议遵守
- 花括号另起一行
- 4 空格缩进
- `"\n"` 输出换行
- 多测框架统一
- 数据结构命名统一
- 树题优先 `dfs/dfs1/dfs2`

### 可灵活处理
- 是否 `#define int long long`
- 使用全局数组还是局部 `vector`
- 是否补特别长的注释
- 是否封装为 `struct`

---

## 九、对未来 Agent 的明确提示

如果你要在这个仓库里继续写题、补模板、修正代码，请始终记住：

> 你的目标不是写“标准教科书答案”，  
> 而是写出**像仓库作者本人会提交的代码**。

具体来说：

- 结构统一
- 命名竞赛化
- 模板朴素实用
- 注释中文、偏题解式
- 能快速提交、快速修改、快速复用

如果出现多种可行写法，优先选择：
1. 与 `Codeforces` 目录现有提交更像的写法
2. 与 `algorithm` 目录现有模板更兼容的写法
3. 作者下一次比赛时最可能继续沿用的写法

---

## 十、补充：风格参考摘要

根据当前仓库观察，作者的稳定风格可以概括为：

- **外层简洁**：统一 `solve + main`
- **内层直接**：算法实现不绕
- **命名朴素**：短变量、经典缩写
- **模板实用**：直接可抄
- **思路清楚**：常在末尾写中文推导
- **兼顾速度与可读性**：明显偏竞赛实战风格

Agent 以后在 `Algorithm` 仓库内生成代码时，应默认以此为最高优先级。