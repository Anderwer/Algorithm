# 比赛知识点总索引

## 说明

本文件用于汇总各场比赛中出现过的算法知识点、常见套路、建模方法与易错点。  
目标不是按题号机械罗列，而是把**相似知识点归并到同一个专题下**，方便后续复习时按算法主题查找。

使用方式：

1. 每做完一场比赛，在对应场次目录下新增或更新 `算法知识点总结.md`
2. 再把这场涉及到的核心知识点同步登记到本总索引
3. 如果知识点和已有内容重复，则优先并入已有专题
4. 如果出现新的、独立性较强的技巧，再新增专题

---

## 一、观察结论 / 过程规律提炼

### 适用题型

- 操作过程题
- 移动 / 传递 / 模拟题
- 看似需要长模拟，但其实答案只由某个关键位置或关键状态决定的题

### 核心思想

这类题通常不需要真的把全过程模拟到底，而是要先问：

- 哪一步开始行为发生本质变化？
- 哪个位置第一次阻止了继续扩展？
- 哪个极值位置真正决定了后续答案？

一旦找到这个“第一次转折点”或“关键代表元”，答案往往就直接出来了。

### 常见结论形式

- 第一个满足某性质的位置
- 最后一个能维持某性质的位置
- 某个前缀 / 后缀长度
- 若干次跳转后落到的关键下标

### 易错点

- 把规律题当成暴力模拟题
- 忽略“第一次变化”往往比“全部变化”更重要
- 对“最左 / 最右 / 第一个 / 最后一个”这种细节处理错误

### 已出现比赛

- `Educational Codeforces Round 188`
  - `A. Passing the Ball`
  - `B. Right Maximum`

---

## 二、倍数统计 / 容斥原理 / gcd-lcm 周期问题

### 适用题型

- 周期性出现
- 多个人 / 多事件按周期发生
- 同时出现、重合出现次数统计
- 倍数个数统计

### 核心思想

如果事件按周期 `x` 发生，那么在区间 `1..m` 内出现次数就是：

$$
\left\lfloor \frac{m}{x} \right\rfloor
$$

如果两个事件周期分别为 `a, b`，同时发生的次数就是：

$$
\left\lfloor \frac{m}{lcm(a, b)} \right\rfloor
$$

三个事件同理：

$$
\left\lfloor \frac{m}{lcm(a, b, c)} \right\rfloor
$$

所以这类题常常是：

- 倍数统计
- `gcd`
- `lcm`
- 容斥原理

绑在一起出现。

### 常见套路

1. 先统计单独发生次数
2. 再统计两两重合次数
3. 再统计三重重合次数
4. 按题意对不同重合层数赋予不同权值

### 易错点

- `lcm(a, b) = a / gcd(a, b) * b` 时溢出
- 忘了三重交要继续算一次
- 没有明确“每种重合情况下每个人 / 每个事件贡献多少”

### 已出现比赛

- `Educational Codeforces Round 188`
  - `C. Spring`

---

## 三、图论建模：二分图判定 / 染色 / 连通块贡献

### 适用题型

- 路径奇偶性交替
- 相邻点需要分成两类
- 边需要满足“左右 / 黑白 / 奇偶层”式约束
- 图中每个连通块独立贡献答案

### 核心思想

如果一个图问题的限制可以抽象成：

- 相邻点必须属于不同类
- 一条路径上奇偶层要满足某种交替关系

那么第一反应就是：

- 这个图 / 这个连通块是否是二分图

### 标准做法

- 用 `col[i] = -1` 表示未染色
- 对每个未访问连通块做 BFS / DFS
- 相邻点染相反颜色
- 如果发现相邻点颜色相同，则不是二分图

### 连通块贡献问题

很多题不是问“整张图是否可行”，而是问：

- 每个可行连通块能贡献多少
- 最终把所有连通块贡献加起来

常见形式：

- `max(cnt[0], cnt[1])`
- `min(cnt[0], cnt[1])`
- `cnt[0] + cnt[1]`
- `cnt[0] * cnt[1]`

具体取哪个，要看题意建模。

### 易错点

- 忘记按连通块分别处理
- 只判了整体图，没统计每块贡献
- 染色时对已访问点判断不严谨
- 没把题意中的“交替”抽象成二分图

### 已出现比赛

- `Educational Codeforces Round 188`
  - `D. Alternating Path`

---

## 四、数字构造 / 数位和链 / 多重集合匹配

### 适用题型

- 重排数字字符
- 构造某种特殊数字串
- 数位和、数位 DP 前置观察
- 给你一堆字符，要求判断 / 构造某种数值过程产生的串

### 核心思想

这类题常常有两个层次：

1. **过程层**
   - 比如 `x -> sumdigits(x) -> ...`
   - 串由若干阶段拼接而成

2. **字符层**
   - 原串只是数字字符 multiset
   - 顺序可以变，但字符频数不能变

于是通常需要：

- 先理解过程结构
- 再把问题转成字符频数匹配
- 必要时做反向构造

### 常见套路

1. 枚举一个关键中间值
2. 生成这个值对应的后缀链 / 过程链
3. 从原字符频数中扣掉这一部分
4. 用剩余字符构造前缀
5. 检查长度、前导零、数字和等条件

### 高频注意事项

- 前导零是否合法
- 整个答案可能就是一个很短的特例
- 不能只校验数字和，还要校验每个数字出现次数
- “能重排成”不等于“原串中顺序要一致”

### 易错点

- 漏掉单字符特判
- 前缀构造时没处理非零首位
- 后缀链合法，但剩余字符无法构成合法前缀
- 只看和，不看频数

### 已出现比赛

- `Educational Codeforces Round 188`
  - `E. Sum of Digits (and Again)`

---

## 五、贪心：比较边际收益

### 适用题型

- 多个对象可以反复操作
- 每次操作收益会变化
- 问若干次操作后的最大 / 最小总收益

### 核心思想

真正重要的不是“操作后总值长什么样”，而是：

- 当前这一步操作在每个对象上的收益分别是多少？
- 哪个对象的下一步收益最大？
- 某个对象是否在任何时刻都不比别的对象差？

如果一个对象的“当前可获得收益”始终不劣于其他对象，那么所有操作都应该优先给它。

### 标准分析方式

1. 先研究单个对象做 `k` 次操作时的最优形式
2. 再拆成“第 1 次、第 2 次、...”的收益序列
3. 比较不同对象的收益序列谁更优
4. 决定是否能贪心地总给某一类对象

### 易错点

- 只看最终形式，不看边际收益
- 没证明“所有操作都扔给同一个对象”是否合法
- 以为要 DP，其实比较增量就够了

### 已出现比赛

- `Educational Codeforces Round 188`
  - `F. Sum of Fractions`

---

## 六、子数组极值贡献 / 单调栈

### 适用题型

- 子数组最小值 / 最大值
- 每个位置作为极值时能覆盖多少子数组
- 子数组答案只和极值代表元有关

### 核心思想

如果子数组的答案只和其最小值 / 最大值相关，就应该立刻想到：

- 统计每个位置作为子数组极值时的贡献次数
- 再按位置贡献求和
- 必要时按数值再做一次合并

### 单调栈经典公式

设某位置 `i` 作为子数组最小值的代表元，其贡献次数通常可写成：

$$
(i - L[i]) \times (R[i] - i)
$$

其中：

- `L[i]`：左边第一个更小（或更小等于）的位置
- `R[i]`：右边第一个更小（或更小等于）的位置

具体用严格还是非严格，要看你如何去重。

### 常见去重方式

统计最小值时常见一组写法：

- 左边弹 `>=`
- 右边弹 `>`

或者反过来选一套对称方案，只要保证每个子数组只被统计一次即可。

### 进阶：按值合并

有些题最终只关心“最小值等于多少”，不关心它来自哪个位置。  
那么可以先按位置统计，再按值合并成：

- `cnt[x] = 最小值恰好为 x 的子数组个数`

### 易错点

- 栈的严格 / 非严格关系写反
- 左右边界去重不统一导致重复或漏算
- 忘了从“按位置贡献”进一步“按数值贡献”合并

### 已出现比赛

- `Educational Codeforces Round 188`
  - `F. Sum of Fractions`

---

## 七、离线处理 / 双指针扫描阈值

### 适用题型

- 查询本身有序
- 答案只和某个阈值前缀有关
- 每个查询只需要维护若干前缀统计量

### 核心思想

如果查询 `k` 已经按非降序给出，那么很多题可以不必每次从头计算，而是：

- 把待统计对象也按关键字排序
- 用指针维护当前满足 `x <= threshold` 的部分
- 只在阈值跨过新元素时更新前缀量

### 常维护的量

- 个数和
- 值的和
- 权值和
- 逆元和
- 某个函数值前缀和

### 适合搭配的题型

- 子数组极值按值合并后
- 数值分类讨论有一条阈值边界时
- 询问只和“前面已激活的部分”有关时

### 易错点

- 忘了查询必须有序才能这么扫
- 指针推进时更新量不全
- 阈值边界是 `<` 还是 `<=` 写错

### 已出现比赛

- `Educational Codeforces Round 188`
  - `F. Sum of Fractions`

---

## 八、模意义分数 / 逆元 / 快速幂

### 适用题型

- 需要在模数下处理分数
- 出现 `1 / a_i`
- 题目要求输出有理数取模后的结果

### 核心思想

如果模数是质数，那么：

$$
a^{-1} \equiv a^{mod-2} \pmod{mod}
$$

所以出现分数时，通常转化成：

- 乘法
- 逆元
- 快速幂

### 常见配套知识

- 模加减乘
- 快速幂
- 费马小定理
- 逆元存在条件

### 易错点

- 模数不是质数时乱用 `a^(mod-2)`
- 中间减法忘了补 `mod`
- 同一个值的逆元重复求太多次，常数偏大

### 已出现比赛

- `Educational Codeforces Round 188`
  - `F. Sum of Fractions`

---

## 九、网格路径建模为区间序列

### 适用题型

- 网格中只允许左右与向下移动
- 关心访问过的格子集合，而不是精确路径顺序
- 每一行的访问状态可以抽象成一个连续块

### 核心思想

如果在同一行只能左右移动，那么这一行访问到的格子集合一定是一个连续区间。  
如果跨行只能往下，那么用到的行一定是前缀若干行。

于是整个路径可以抽象成一串区间：

$$
[l_1, r_1], [l_2, r_2], \dots, [l_h, r_h]
$$

并满足相邻两行区间必须可连通。

### 常见后续步骤

- 证明每行状态是区间
- 证明相邻两行区间必须相交
- 把原网格问题转化为区间序列计数问题

### 易错点

- 只直觉认为“是一段”，但没有基于移动方式确认
- 没看出“顺序不重要”时，状态可以只保留访问集合
- 忽视首行、起点所在位置带来的特殊限制

### 已出现比赛

- `Educational Codeforces Round 188`
  - `G. Grid Path`

---

## 十、相交计数转补集计数

### 适用题型

- 区间之间是否相交
- 需要统计“所有与当前对象相容的旧状态”
- 直接枚举相交对象太慢

### 核心思想

统计“与 `[x, y]` 相交的所有区间”时，常常可以用：

- 总数
- 减去左边完全分离的部分
- 减去右边完全分离的部分

因为区间不相交当且仅当：

- `r < x`
- 或 `l > y`

所以相交计数往往能被拆成几个前缀 / 后缀量。

### 适合维护的辅助量

- 总和 `T`
- 右端点过小的前缀量
- 左端点过大的后缀量

### 易错点

- 补集拆分不完整
- 两种不相交情况边界写错
- 前缀量和后缀量的定义与实际使用不匹配

### 已出现比赛

- `Educational Codeforces Round 188`
  - `G. Grid Path`

---

## 十一、状态压缩：从二维状态到线性小维状态

### 适用题型

- 原始 DP 状态看起来是二维 / 三维
- 但下一步只依赖于少量聚合量
- 大步数、不能直接逐层 DP

### 核心思想

如果发现：

- 所有新状态都只依赖某些总和
- 或依赖于若干前缀 / 后缀聚合量
- 而不依赖于原状态数组的完整细节

那就说明状态可以压缩。

本质上是：

> 下一层不关心“你来自哪个具体状态”，只关心“你属于哪个统计类”。

### 常见压缩形式

- 总状态和
- 前缀和 / 后缀和
- 按左端点 / 右端点聚合后的数组
- 若干矩统计量

### 易错点

- 只会写原始状态，没去想新状态到底依赖什么
- 辅助量定义不闭合，导致下一步还需要原始数组
- 压缩后状态转移式不够线性

### 已出现比赛

- `Educational Codeforces Round 188`
  - `G. Grid Path`

---

## 十二、线性转移 / 矩阵快速幂

### 适用题型

- 状态数不大
- 转移是线性的
- 步数非常大，普通 DP 做不了

### 核心思想

如果状态满足：

$$
f_{t+1} = M \cdot f_t
$$

且 `t` 很大，那么就要考虑：

$$
f_t = M^{t-1} \cdot f_1
$$

这就是标准矩阵快速幂模型。

### 识别信号

- 下一步每个状态是旧状态的线性组合
- 状态维度不大（几十到几百）
- 步数可达 `1e9`、`1e18` 等级
- 不能逐层转移

### 常见搭配

- 状态压缩
- 计数 DP
- 自动机 DP
- 网格 / 图上的线性状态

### 易错点

- 状态还没压小就硬上矩阵，导致维数爆炸
- 转移不是线性的却误套矩阵
- 矩阵大小和答案向量定义错位

### 已出现比赛

- `Educational Codeforces Round 188`
  - `G. Grid Path`

---

## 十三、矩阵快速幂常数优化

### 适用题型

- 理论复杂度可过
- 但朴素矩阵实现容易 TLE

### 常见优化

#### 1. 不先完整求幂矩阵
如果最终只需要：

$$
M^k \cdot f
$$

那就没必要先求出完整的 `M^k` 再乘一次向量，  
可以在快速幂过程中直接把矩阵作用到向量上。

#### 2. 右矩阵转置
矩阵乘法里常用：

- 先把右矩阵转置
- 再做行和行的点积

这样缓存命中更高。

#### 3. 减少取模次数
如果中间不会溢出太严重，可以：

- 先累加
- 最后统一 `% mod`

或每累计一段再取模一次。

#### 4. 减少重复分配内存
矩阵很大时，频繁创建临时对象常数也很高。

### 易错点

- 优化了但破坏了正确性
- 减少 `% mod` 时溢出
- 只顾优化乘法，却忽略幂里其实还可以省一次矩阵乘答案

### 已出现比赛

- `Educational Codeforces Round 188`
  - `G. Grid Path`

---

## 十四、已登记比赛索引

### Educational Codeforces Round 188
已覆盖知识点：

- 观察结论 / 关键位置提炼
- 倍数统计 / 容斥 / `gcd-lcm`
- 二分图判定 / 连通块贡献
- 数字构造 / 数位和链 / 频数匹配
- 贪心边际收益
- 子数组最小值贡献 / 单调栈
- 离线双指针阈值扫描
- 模逆元 / 快速幂
- 网格路径转区间序列
- 相交计数转补集计数
- 状态压缩
- 矩阵快速幂与常数优化

---

## 十五、后续维护规则

以后每做完一场比赛，按以下步骤更新本文件：

1. 先判断这场题出现了哪些算法专题
2. 如果是已有专题：
   - 只在对应专题下补充“已出现比赛”
   - 必要时补新易错点 / 新套路
3. 如果是新专题：
   - 新开一级标题
4. 最后在“已登记比赛索引”里补一条这场比赛的覆盖内容

---

## 十六、当前复习优先级建议

如果以后想按专题复习，建议优先级如下：

### 第一优先级
- 子数组极值贡献 / 单调栈
- 贪心比较边际收益
- 状态压缩 + 矩阵快速幂

### 第二优先级
- 二分图判定 / 连通块贡献
- 离线双指针扫描阈值
- 数字构造 / 频数匹配

### 第三优先级
- 观察规律题
- 倍数统计 / 容斥
- 矩阵常数优化

---

## 十七、结语

这份总索引的目的，是把“做过的比赛”逐步沉淀成“会复用的算法专题”。  
以后继续做题时，尽量把：

- 同类建模
- 同类数据结构
- 同类计数套路
- 同类优化方法

不断合并到这份总表里，最终形成自己的竞赛知识地图。